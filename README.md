#Docker configuration for Symfony 5.4

##Installation :

First step :\
change db name in docker-compose.yml

Second Step :\
copy your application files in app folder

Third step :
```
APP_NAME=YOUR_APP_NAME docker-compose up -d --build
```